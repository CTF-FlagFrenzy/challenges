version: '3'

volumes:
  my-django-data:

services:
  flask:
    build:
      context: .
      dockerfile: flask_app/Dockerfile

    ports:
      - '8003:8003'

    environment:
      - TEAMKEY=XXXXXXX
      - CHALLENGEKEY=3fn@Yu7L58
